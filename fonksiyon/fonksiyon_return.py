#fonksiyonlarda return

def toplam(a, b):
    return a + b # a + b sonucunu döndürür

topla = toplam(3, 5)
print(topla)

#fonksiyonda return'dan sonraki satırlar okunmaz
#bu sebeple genelde return son satırda olmalıdır
